# 生成矩阵示例
rows = 3
cols = 4
matrix = []

for i in range(rows):
    row = []
    for j in range(cols):
        element = i * cols + j + 1
        row.append(element)
    matrix.append(row)

# 打印生成的矩阵
for row in matrix:
    print(row)
